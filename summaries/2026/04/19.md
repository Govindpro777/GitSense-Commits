# Activity Summary for 4/19/2026

## 1:44:20 PM
The file `c:\Users\91965\Desktop\MikeLegal\clm-frontend\shared\src\lib\utils\hooks\specific\useContributors.ts` was updated twice within a short timeframe.

The initial update at 4/19/2026, 1:17:28 PM, introduced a new React hook named `useContributors`. This hook is designed to fetch a list of contributors using an Apollo Client GraphQL query called `searchContributors`. The query accepts several variables like `excludeOutsideCollaborator`, `excludeTeamId`, `excludeUserId`, and `searchText`, specifically filtering for `productType: CONTRACT`. The hook utilizes `accountsClient` and has a `fetchPolicy` of `cache-and-network`. A notable feature of this initial implementation is the inclusion of logic to skip the query execution if the user is identified as an external signatory (checking `window.location.href` for 'ext=' and `sessionStorage` for 'external_signatory'). It also defines TypeScript types for `Contributor` and `GetAllContributorsResponse`.

A subsequent, very minor update occurred at 4/19/2026, 1:17:45 PM, on the exact same file. This change specifically added `errorPolicy: "all"` to the `useQuery` configuration within the `useContributors` hook. This modification dictates that all errors, including GraphQL errors, will be returned to the component, rather than potentially being suppressed or causing the query to throw an unhandled error.

In summary, the key change was the introduction of the `useContributors` hook with its core logic for fetching contributors and handling external signatory authentication, followed by a quick refinement to its error handling policy.