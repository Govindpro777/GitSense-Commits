# Activity Summary for 4/3/2026

## 12:58:54 PM
The code changes primarily focus on styling updates and the development of a "Deadline Management" feature within an application named "IPSuite-Web."

**File-Specific Updates:**

1.  **`c:\Users\91965\Desktop\MikeLegal\ipsuit2\IPSuite-Web\src\stylesheets\App.css`**:
    *   Initially (4/3/2026, 11:07:13 AM), this file defines extensive global CSS, importing Tailwind CSS and `tw-animate-css`. It includes styles for core application elements, various table and card components (e.g., `General-Table`, `General-Mark-Card`), navigation, tooltips, and specific overrides for Ant Design components, particularly for table headers (`.notes-nested-table`, `.exam-report-threat-table`). It also contains print media queries and Tailwind `@layer utilities` for Ant Design Sider and Menu item styling.
    *   A significant update occurred on 4/3/2026, 11:07:41 AM, introducing new styles:
        *   `.no-card-padding .ant-card-body` to remove default padding from Ant Design card bodies.
        *   `.round-checkbox` to transform Ant Design checkboxes into circular shapes, handling various states like checked, hover, and focus.
        *   `.custom-tabs .ant-tabs-nav` to make Ant Design tabs sticky at the top with a white background.
    *   This file shows a recurring pattern of these new styles being added (e.g., at 11:07:41 AM, 11:24:31 AM, 11:33:03 AM, 11:35:21 AM) and then seemingly reverted or saved without these changes (e.g., at 11:24:18 AM, 11:32:46 AM, 11:35:13 AM).

2.  **`c:\Users\91965\Desktop\MikeLegal\ipsuit2\IPSuite-Web\src\components\managerComponents\deadlineManagement\DeadlineManagement.jsx`**:
    *   First committed on 4/3/2026, 11:28:50 AM, this new React component sets up the primary interface for deadline management. It initializes various states based on the URL and user subscriptions, manages filtering logic for trademarks (e.g., by class, status, dates, proprietor), and integrates with Redux for data fetching and state management. The initial page title rendering used Ant Design's `Row` and `Col` components.
    *   A subsequent update on 4/3/2026, 11:30:10 AM, refactored the `pageTitle` rendering. The Ant Design `Row` and `Col` components were replaced with a `div`-based structure utilizing Tailwind CSS utility classes, indicating a move towards a more flexible and utility-first layout approach for this section.

3.  **`c:\Users\91965\Desktop\MikeLegal\ipsuit2\IPSuite-Web\src\components\managerComponents\deadlineManagement\components\DeadlineContent.jsx`**:
    *   This new React component, appearing on 4/3/2026, 11:30:21 AM, is likely a child component of `DeadlineManagement`. It's responsible for displaying the actual deadline data in a table. Key functionalities include handling table pagination, row selection (including a "Deselect All Data" option), dynamic table height adjustment using `ResizeObserver`, and various interactive actions such as creating tasks, managing notes, sending emails, and updating reply/client statuses via modals. It also dispatches specific Redux actions to update the backend.

**Timestamps of Significant Changes:**

*   **4/3/2026, 11:07:41 AM:** New CSS styles (`.no-card-padding`, `.round-checkbox`, `.custom-tabs`) were introduced in `App.css`.
*   **4/3/2026, 11:28:50 AM:** The `DeadlineManagement.jsx` component, central to the deadline management feature, was initially logged.
*   **4/3/2026, 11:30:10 AM:** The `pageTitle` layout in `DeadlineManagement.jsx` was refactored from Ant Design grid to Tailwind CSS flexbox.
*   **4/3/2026, 11:30:21 AM:** The `DeadlineContent.jsx` component, detailing the interactive table for deadline data, was introduced.

**Patterns or Recurring Elements:**

*   **Hybrid CSS Approach:** The project uses a combination of traditional CSS, Tailwind CSS for utilities, and Ant Design's component-based styling, often with custom overrides to tailor Ant Design components. The refactoring in `DeadlineManagement.jsx` from Ant Design `Row`/`Col` to Tailwind `div`s suggests a strategic shift or preference for Tailwind's utility-first approach in certain UI areas.
*   **Ant Design UI Framework:** There is extensive reliance on Ant Design components (Tabs, Typography, Table, Modal, notification, icons) for building the user interface across both JavaScript and CSS files.
*   **Redux for State Management:** Both `DeadlineManagement.jsx` and `DeadlineContent.jsx` consistently use `react-redux` hooks (`useDispatch`, `useSelector`) to manage application state, fetch data, and trigger actions, indicating a centralized state management pattern.
*   **Modular Component Design:** The deadline management functionality is split into a parent `DeadlineManagement` component and a child `DeadlineContent` component, demonstrating a modular and hierarchical component structure.
*   **Interactive Data Management:** A common theme is providing robust user interaction capabilities around data, including filtering, selection (single and bulk), viewing details (profiles, reasons for notice, PDFs), and performing actions (add task, notes, email, update statuses) via modals.
*   **CSS Reversion/Restoration:** The `App.css` file exhibits a peculiar pattern of specific style additions being repeatedly added, then removed/reverted, and subsequently re-added within a short timeframe. This could indicate frequent saving, conflicting changes, or experimental feature toggling during development.

## 1:12:30 PM
This log details an update to the `src/Pages/CreateNewSalesInvoice.jsx` file, timestamped `4/3/2026, 10:12:28 AM`.

The changes focus on enhancing a React component responsible for creating and editing sales invoices. Key updates include:

*   **Comprehensive State Management:** The component now manages an extensive set of states covering customer details (ID, address), invoice metadata (number, date, notes, book account), multiple item rows (with quantity, rate, amount), tax percentages (CGST, SGST, IGST, discount), and detailed payment information (UPI, bank transfer, cheque, card details).
*   **Dynamic Data Fetching:** On component mount, it fetches `items` and `account-groups` from an API using `axios` and a `Bearer token`. It specifically filters `accountGroups` to identify "Sundry Debtors" and "Sundry Creditors" for customer selection and "Sales accounts" for the `bookNameAccountId`.
*   **Robust Edit Mode:** The `useEffect` hook handles populating the form when `isEditMode` is active, extracting data from `editData`. This includes mapping existing invoice items to the `rows` state and populating the extensive `paymentDetails` object. It also intelligently calculates `cgstPercent`, `sgstPercent`, and `discountPercent` based on stored tax/discount amounts if percentages aren't directly available.
*   **Dynamic Item Row Management:** Functionality for adding (`addRow`), removing (`removeRow`), and modifying (`handleRowChange`) invoice item rows is implemented. When an `itemId` is selected, the component automatically populates other item details like `itemName`, `itemCode`, `propertyType`, `projectName`, `area`, and `rate` from the `itemsList`.
*   **Real-time Calculations:** The `subTotal`, `discountAmount` (based on `discountPercent`), and `taxableAmount` are calculated dynamically. The snippet ends while in the process of defining `cgst`, `sgst`, `igst`, and `tax` calculations based on the `taxableAmount`.
*   **API Integration and UI Feedback:** Consistent use of `axios` for backend communication and `react-toastify` for user feedback on API call success or failure.

**Patterns and Recurring Elements:**

*   Heavy reliance on React's `useState` for managing various independent and interconnected pieces of form data.
*   Frequent use of `useEffect` for data fetching on component mount and for populating data in edit mode.
*   Standardized API interaction using `axios` with `Bearer token` authentication.
*   Calculations for financial figures (amount, subtotal, discount, tax) are dynamically derived from user inputs and state changes.
*   The component is designed for comprehensive data input, covering almost all aspects of a sales invoice, including detailed payment methods.

## 1:14:13 PM
The provided log details code changes across two main types of files: VS Code user settings and React components for a frontend application.

### File-Specific Updates:

*   **`c:\Users\91965\AppData\Roaming\Code\User\settings.json`**
    *   **Initial Configuration (4/3/2026, 10:13:57 AM):** A comprehensive set of VS Code settings were configured. These included UI theme (`Visual Studio Dark - C++`), C/C++ update channel (`Insiders`), `code-runner` behavior (run in terminal, save before run), editor features (mouse wheel zoom, accessibility support off, live server tag verification), Python formatting, JavaScript import handling, editor hints, C/C++ code analysis delay, Git Graph and Git Lens accessibility, explorer drag-and-drop confirmation, and default formatters (Prettier for general, TypeScript language features for `javascriptreact`, `html`, and `typescriptreact`). Notably, `files.autoSave` was initially set to `"afterDelay"`.
    *   **Auto-Save Adjustment (4/3/2026, 10:14:32 AM):** The `files.autoSave` setting was explicitly changed from `"afterDelay"` to `"off"`, with the previous "afterDelay" line commented out.

*   **`c:\Users\91965\Desktop\MikeLegal\clm-frontend\apps\MikeCLM\src\containers\Dashboard\Dashboard.tsx`**
    *   **Dashboard Component Development (4/3/2026, 10:14:13 AM):** This commit introduced a detailed React Dashboard component. It imports a wide array of UI components from Ant Design, Recharts, Lucide React, and custom UI libraries (e.g., `Card`, `Button`, `Tooltip`). The component includes logic for fetching contract status insights using GraphQL (`useLazyQuery` with `GET_STATUS_INSIGHTS`), managing UI state (e.g., `isDrawerOpen`, `isWelcomeOpen`, `isWidgetsOpen`), and interacting with local storage to control a welcome modal and dashboard widgets. It renders a personalized welcome message, an "Edit Widgets" button, and a core "Agreement Status & Execution" widget displaying a PieChart of contract statuses.

*   **`c:\Users\91965\Desktop\MikeLegal\clm-frontend\apps\MikeCLM\src\containers\Drafts\Drafts.tsx`**
    *   **Initial Table Setup (4/3/2026, 10:22:40 AM):** The Drafts page component was initialized with an Ant Design `Table` to display contract records. It defined data interfaces (`Owner`, `ContractRecord`), a small set of `demoContracts`, a `StageBadge` component for visual status, and basic UI elements like search and filter placeholders. It also included initial definitions for bulk `applyMenuItems` and individual `rowActionMenu` items.
    *   **Filter Integration & Refinement (4/3/2026, 10:23:03 AM - 4/3/2026, 10:28:39 AM & 10:49:44 AM):**
        *   Predefined lists (`ownersList`, `stagesList`, `datesList`, `contractNamesList`) were added (10:23:03 AM).
        *   The generic `FilterDropdown` component was replaced by `FiltersDropdown` (10:24:58 AM), which was then integrated into the "Contract Name", "Owner", and "Last Updated" table columns to enable dynamic filtering.
        *   The `handleFilterChange` function logic was refined for robust array handling of filter values (10:26:50 AM).
        *   An `isFilterApplied` helper function was added (10:49:44 AM).
    *   **Pagination and Search Bar Component Refactoring (4/3/2026, 1:04:23 PM - 4/3/2026, 1:08:07 PM):**
        *   The custom `ContractsPagination` component was removed from imports and replaced with Ant Design's native `Pagination` component (1:04:23 PM, 1:05:29 PM).
        *   The `ContractsSearchBar` component was removed from general imports and replaced by a more specific `SearchBar` component imported from `../../components/UI/SearchBar/SearchBar` (1:06:39 PM, 1:08:07 PM). A redundant `SearchBar` line was also cleaned up (1:06:49 PM).
    *   **Row Action Menu Update (4/3/2026, 1:09:44 PM):** The `rowActionMenu` items for individual contracts were updated; "Edit" was replaced by "Share" and "Send for Review" was replaced by "Finalize Draft".
    *   **Demo Data Expansion (4/3/2026, 1:10:44 PM - 4/3/2026, 1:11:28 PM):** The `demoContracts` array, used for sample data in the table, was progressively expanded from 3 to 8 entries.

### Patterns and Recurring Elements:

*   **Iterative Development:** The `Drafts.tsx` file shows a clear pattern of iterative development, where core functionality (table, actions) is first established, followed by enhancements like filter integration, component refactoring, and data expansion.
*   **Ant Design Integration:** Both `Dashboard.tsx` and `Drafts.tsx` heavily rely on Ant Design components for UI, indicating a consistent design system across the application.
*   **State Management:** Frequent use of React's `useState` hook for managing UI state (e.g., drawer visibility, search queries, selected items, filter values) is evident.
*   **Component Modularity:** The application utilizes a modular component structure, importing various helper components from `../../components` and `../../components/UI`, such as `FiltersDropdown` and `SearchBar`.
*   **Styling Convention:** The use of explicit `!important` Tailwind CSS classes (e.g., `!bg-blue-50`) is a recurring pattern in the `Drafts.tsx` component's `StageBadge` and `TABLE_CLS` definitions.
*   **Filter Logic:** The `handleFilterChange` function and the structure of `filterValues` state demonstrate a standardized approach to handling multiple filter criteria.
*   **Timestamp Clustering:** There are distinct clusters of activity around the VS Code settings (around 10:13-10:14 AM) and then a concentrated period of development on the `Drafts.tsx` component, with numerous small changes occurring within minutes of each other (10:22 AM - 10:28 AM and 1:04 PM - 1:12 PM). This suggests active development sessions on these specific files.

## 2:12:37 PM
The development log details changes across two key React components within the `webseeder-erp-main` project.

**File-specific updates:**

*   **`c:\Users\91965\Desktop\WEB\webseeder-erp-main\src\Pages\CreateNewSalesInvoice.jsx` (Timestamp: 4/3/2026, 10:12:28 AM)**
    *   This component handles the creation and editing of sales invoices.
    *   It manages a comprehensive set of state variables for customer information, invoice details (including manual invoice number override, date, notes), an itemized list with quantities, rates, and amounts, and tax percentages (CGST, SGST, IGST) and discount percentages.
    *   Payment details are extensively managed, supporting UPI, bank, cheque, and card information.
    *   The component fetches `items` and `account-groups` (filtering for "Sales accounts", "Sundry Debtors", and "Sundry Creditors") from the backend API upon initialization.
    *   It features an "edit mode" that pre-populates the form with existing invoice data, including logic to calculate tax and discount percentages from stored values if necessary.
    *   There are handlers for dynamically adding, removing, and updating rows in the itemized list, with automatic calculation of item amounts and updates to item details based on selected `itemId`.
    *   The overall subtotal, discount amount, taxable amount, and GST calculations are managed reactively.

*   **`c:\Users\91965\Desktop\WEB\webseeder-erp-main\src\Pages\CreateAccountForm.jsx` (Timestamps: 4/3/2026, 1:55:50 PM, 1:56:50 PM, 1:57:45 PM, 1:58:08 PM, 1:59:03 PM)**
    *   This component is responsible for creating and managing various account groups.
    *   It defines a detailed `initialFormState` covering general account info, contact details, statutory information (GSTIN, PAN), bank details, and credit limit settings, including flags for enabling/providing these details.
    *   The component uses shared constants for common Tailwind CSS classes (`inputCls`, `selectCls`) and a generic `Field` component for consistent form field rendering.
    *   A specialized `UnderGroupDropdown` component is implemented, providing a searchable dropdown for selecting chart accounts with an option to create a new one. This dropdown also handles closing on outside clicks.
    *   State management includes lists of `accountGroups` and `chartAccounts`, form data, editing status, loading indicators, confirmation dialogs, view management (list vs. form), active tabs, and pagination details.
    *   It fetches paginated and filterable `account-groups` and `chart-of-accounts` from the API.
    *   URL query parameters (`createaccount=true`) can directly open the form for creating a new account.
    *   A debounced search term (`debouncedSearchTerm`) is used to optimize API calls when a user types in the search field.
    *   Conditional logic (`isLoanGroup`, `isBankGroup`, `isSundryDebtors`, `isSundryCreditors`) suggests dynamic display or validation based on the selected "Under Group".
    *   The form `handleChange` function supports updating nested state objects and converting input values based on type (e.g., checkbox to boolean, number input to Number).

**Timestamps of Significant Changes:**
The timestamp for `CreateNewSalesInvoice.jsx` (4/3/2026, 10:12:28 AM) indicates a single, comprehensive update to the sales invoice creation and editing functionality. The subsequent close-together timestamps for `CreateAccountForm.jsx` (between 1:55:50 PM and 1:59:03 PM on the same date) suggest a period of active work or frequent saving on the account form, though the provided code snippets are functionally identical across these entries, implying no major *functional* changes in the logged portion during this specific interval.

**Patterns or Recurring Elements:**
*   **React Ecosystem:** Both files are React components, heavily utilizing `useState` and `useEffect` hooks for local state management and side effects.
*   **API Interactions:** `axios` is consistently used for making HTTP requests to a backend API.
*   **Authentication:** Both components retrieve an authorization `token` from `localStorage` and include it in API request headers, indicating a standardized authentication approach.
*   **Environment Variables:** API base URLs are retrieved from `import.meta.env.VITE_API_BASE_URL`, suggesting a Vite development environment.
*   **User Feedback:** `react-toastify` is used in both for displaying success or error messages to the user.
*   **Routing:** `react-router-dom` (`useNavigate`, `useLocation`) is present for navigation and accessing URL information.
*   **Icon Library:** Both components import icons from `lucide-react`, indicating a consistent icon set across the application.
*   **Data Structure:** Both deal with complex form data, often involving nested objects (e.g., `contact`, `bankDetails`, `items` array).
*   **Immutability:** State updates typically follow functional updates and spread syntax to maintain immutability.

## 2:13:53 PM
The provided log details changes across three distinct files: a VS Code user `settings.json` file, and two React/TypeScript files (`Dashboard.tsx` and `Drafts.tsx`) within a frontend application named `MikeCLM`.

**File-Specific Updates:**

1.  **`c:\Users\91965\AppData\Roaming\Code\User\settings.json`**
    *   **Timestamp Range:** 4/3/2026, 10:13:57 AM to 4/3/2026, 10:14:32 AM
    *   **Key Changes:** This file, likely a VS Code settings configuration, shows minimal changes. A specific line `files.autoSave: "off"` was commented out (`// "files.autoSave": "off"`), indicating a shift to explicitly disable auto-save behavior or at least remove a redundant setting at 10:14:32 AM. Other settings like color theme, C/C++ update channel, code runner behavior, editor word wrap, mouse wheel zoom, Live Share, Live Server, Python formatting, JavaScript import updates, empty editor hints, C/C++ code analysis delay, Git Graph/Lens settings, drag-and-drop confirmation, default formatter (Prettier, then TypeScript/HTML), Copilot suggestions, and insert spaces remained consistent across these entries.

2.  **`c:\Users\91965\Desktop\MikeLegal\clm-frontend\apps\MikeCLM\src\containers\Dashboard\Dashboard.tsx`**
    *   **Timestamp:** 4/3/2026, 10:14:13 AM
    *   **Key Changes:** This file appears to be a React component for a dashboard. The provided snippet is primarily concerned with fetching and displaying contract insights, specifically a "Status & Execution" pie chart.
        *   It imports various Ant Design components (`Link`, `message`), shared utilities (`useDashboardUtils`, `userDataSelector`, `userIdSelector`, `DASHBOARD_WIDGETS`, `GET_STATUS_INSIGHTS`, `WELCOME_MODAL_CLM`), Lucide React icons, and Recharts components.
        *   It uses `react-redux` for state management (`userId`, `userData`) and `react-router-dom` for navigation.
        *   State variables (`isDrawerOpen`, `isWelcomeOpen`, `statusInsights`, `isWidgetsOpen`, `items`) are managed using `useState`.
        *   A `useLazyQuery` hook is set up to fetch `GET_STATUS_INSIGHTS` with `onCompleted` and `onError` handlers to process contract status data, including transforming it into a `TransformedStatus` array and setting status colors.
        *   `useEffect` hooks handle initial data fetching (`getInsightsData()`), loading stored dashboard widgets from `localStorage`, and managing the display of a welcome modal based on `userId` and `localStorage`.
        *   Helper functions (`handleWelcomeCta`, `getCategoryData`, `getObligationData`, `getDashboardTitle`) are defined for specific dashboard logic.
        *   The `InfoTooltip` component is used to provide contextual help.
        *   The JSX renders a `WelcomeModal`, a dashboard header displaying user name and offering an "Edit Widgets" button, and then dynamically maps through `items` to render different widgets. The provided content specifically shows the "STATUS_EXECUTION" widget, which includes the `PieChart` for contract status breakdown.

3.  **`c:\Users\91965\Desktop\MikeLegal\clm-frontend\apps\MikeCLM\src\containers\Drafts\Drafts.tsx`**
    *   **Timestamp Range:** 4/3/2026, 10:22:40 AM to 4/3/2026, 1:46:16 PM
    *   **Key Changes:** This file is a React component for managing "Drafts" in a contract lifecycle management (CLM) application. The changes mostly involve refining table column filters, introducing a new component, and adjusting Ant Design component imports/usage over an extended period.
        *   **Initial Structure:** The component starts with basic imports from `antd` for table, buttons, checkboxes, layout, dropdowns, avatars, and icons (`@ant-design/icons`). It defines interfaces for `Owner` and `ContractRecord` and provides `demoContracts` data. A `StageBadge` component for visual representation of contract stages is included.
        *   **Table Styling and Actions:** `applyMenuItems` and `rowActionMenu` arrays define actions for bulk operations and individual row actions. `statusCards` define overview metrics for different contract stages. `TABLE_CLS` concatenates Tailwind CSS classes for table styling.
        *   **State Management:** Multiple `useState` hooks manage UI state like active card, search queries, filter values, pending changes, report drawer visibility, selected rows (`selectedIds`, `checkAll`), and pagination (`currentPage`, `pageSize`).
        *   **Filtering Logic:** `handleFilterChange` updates filter values, and `isFilterApplied` (added around 10:49:44 AM) checks if a filter is active. `ownersList`, `stagesList`, `datesList`, and `contractNamesList` are defined to populate filter dropdowns.
        *   **Table Columns Evolution:**
            *   **10:22:40 AM - 10:23:03 AM:** The initial implementation uses `<NewFilterIcon />` as a placeholder in column headers for filtering.
            *   **10:24:58 AM - 10:25:28 AM:** The `FilterDropdown` component import changes to `FiltersDropdown`.
            *   **10:25:04 AM - 10:52:05 AM:** FilterDropdowns are actively integrated into the column headers for "Contract Name", "Owner", and "Last Updated", allowing users to filter by these criteria. The `contractNamesList`, `ownersList`, and `datesList` are used to populate these dropdowns.
            *   **1:04:23 PM:** `Pagination` is added to the `antd` import list, suggesting an upcoming or concurrent implementation of table pagination.
            *   **1:06:39 PM - 1:08:14 PM:** `SearchBar` component is imported and referenced, indicating a dedicated search input. `ContractsPagination` import is removed, likely replaced by `antd`'s `Pagination` component.
            *   **1:09:44 PM - 1:10:10 PM:** The `rowActionMenu` is updated, removing "Edit" and "Review" options and adding "Finalize Draft". This implies a change in the draft workflow where direct editing from the table row might be removed or moved elsewhere, and "Finalize Draft" is introduced.
            *   **1:11:28 PM:** The `demoContracts` array is expanded from 3 to 8 entries, indicating an increase in sample data for testing or demonstration purposes.
            *   **1:17:23 PM:** `Badge` and `Tooltip` are added to the Ant Design imports.
            *   **1:17:47 PM:** `InfoCircleOutlined` icon is added to the Ant Design imports.
            *   **1:34:12 PM - 1:34:29 PM:** The `useNavigate` hook from `react-router-dom` is moved from an `// inside component` comment to being actually called within the `Drafts` functional component, indicating its active use for programmatic navigation.
            *   **1:37:29 PM - 1:38:52 PM:** The `TABLE_CLS` array is slightly modified, specifically removing `"[&_.ant-table]:!rounded-none [&_.ant-table-container]:!rounded-none"` from its definition at 1:38:10 PM and then re-adding it (implicitly by not removing it again in subsequent logs). More importantly, `"[&_.ant-table-tbody_td]:!vertical-align-top"` is also removed from `TABLE_CLS` at 1:38:10 PM, then the `TABLE_CLS` definition seems to stabilize.
            *   **1:41:14 PM:** A minor CSS class adjustment in `TABLE_CLS`, changing a hover effect from `!bg-blue-50/50` to `!bg-blue-100/50`.
            *   **1:42:37 PM - 1:42:54 PM:** The `fileName` for `demoContracts[0]` is expanded, making it a much longer string, likely for testing how long contract names render in the table.

**Patterns and Recurring Elements:**

*   **Ant Design (antd) Components:** Heavily used across both `Dashboard.tsx` and `Drafts.tsx` for UI elements like `Table`, `Button`, `Checkbox`, `Layout`, `Dropdown`, `Avatar`, `Pagination`, `Modal`, `Tooltip`, `Badge`.
*   **React Hooks:** `useState` is consistently used for managing local component state (e.g., `isDrawerOpen`, `selectedIds`, `filterValues`). `useEffect` is used for side effects like data fetching and local storage interactions. `useNavigate` and `useLocation` from `react-router-dom` are used for navigation.
*   **Routing:** The `router.tsx` file defines application routes using `react-router-dom`'s `createBrowserRouter`. A `UploadTemplate` route is added.
*   **Filtering Mechanism:** In `Drafts.tsx`, there's a clear pattern of implementing filters for table columns:
    *   Each filterable column title includes a `NewFilterIcon`.
    *   A `FiltersDropdown` component is used to handle the filtering logic, taking `data`, `placeholder`, `handleChange`, `filter`, `values`, `mode`, and `isExtended` props.
    *   `filterValues` state stores the active filters for various keys (`fileName`, `owner`, `lastUpdated`, `stage`, `typeOfAgreement`).
*   **Local Storage Usage:** Both `Dashboard.tsx` and `NavigationBar.tsx` interact with `localStorage` to persist UI preferences or user states (e.g., `dashboard_widgets`, `WELCOME_MODAL_CLM`, `navbar_collapse`).
*   **Styling:** Tailwind CSS classes are extensively used, indicated by the `!` prefix for important styles and `cn` utility for conditional class application.
*   **Data Structure:** Both `Dashboard.tsx` and `Drafts.tsx` deal with structured data (contracts, insights) and define clear TypeScript interfaces (`TransformedStatus`, `Owner`, `ContractRecord`) for type safety.
*   **Error Handling:** In `Dashboard.tsx`, GraphQL queries include `onError` callbacks to log errors and display user-friendly messages.
*   **Navigation Bar Configuration:** The `NavigationBar.tsx` component defines a static `navigationList` array that dictates the primary navigation links within the application, including Dashboard, Drafts, Contracts, Tasks, Calendar, Report, and Settings. It also manages a collapse state for the sidebar and integrates external links for other Mike Legal products.

**Significant Timestamps:**

*   **4/3/2026, 10:14:13 AM:** Initial state of `Dashboard.tsx` showing complex data fetching and rendering logic for contract insights.
*   **4/3/2026, 10:22:40 AM:** Initial state of `Drafts.tsx` showing a table structure with basic filtering placeholders.
*   **4/3/2026, 10:24:58 AM - 10:52:05 AM:** Introduction and refinement of `FiltersDropdown` components in `Drafts.tsx` to enable interactive filtering on table columns.
*   **4/3/2026, 1:06:39 PM:** `SearchBar` component is integrated into `Drafts.tsx`, and `ContractsPagination` import is removed, signifying a shift in pagination implementation.
*   **4/3/2026, 1:09:44 PM - 1:10:10 PM:** Workflow change in `Drafts.tsx` to streamline row actions, removing "Edit" and "Review" in favor of "Finalize Draft".
*   **4/3/2026, 1:11:28 PM:** Expansion of sample `demoContracts` data in `Drafts.tsx`.
*   **4/3/2026, 1:17:23 PM - 1:17:47 PM:** Addition of Ant Design `Badge`, `Tooltip`, and `InfoCircleOutlined` icons in `Drafts.tsx` for enhanced UI.
*   **4/3/2026, 1:26:48 PM:** Comprehensive update to `NavigationBar.tsx`, defining navigation items, handling collapsed state, and integrating external links to other products with tooltips for access control.
*   **4/3/2026, 1:30:09 PM - 1:31:27 PM:** Introduction of a new `UploadTemplate.tsx` component, its inclusion in `src/containers/index.ts`, and its integration into the main application router (`src/configs/router.tsx`) as a new route.
*   **4/3/2026, 1:34:12 PM:** Active usage of `useNavigate` hook in `Drafts.tsx`.
*   **4/3/2026, 1:41:14 PM:** Minor styling adjustment for table row hover effect in `Drafts.tsx`.
*   **4/3/2026, 1:42:37 PM:** Adjustment of sample data (`fileName`) in `Drafts.tsx` for testing purposes.