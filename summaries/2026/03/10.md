# Activity Summary for 3/10/2026

## 10:18:07 AM
The provided code changes, all timestamped between 12:05 AM and 12:49 AM on March 10, 2026, focus on core functionalities for a "Sales ERP Management System," particularly around purchase and sales transactions, and API documentation.

### File-Specific Updates:

*   **`c:\Users\91965\Desktop\WEB\SALES_ERP\swagger.js`** (Timestamp: 3/10/2026, 12:05:54 AM)
    This file configures Swagger for API documentation, defining the API title ("SalesERP API"), description, host (`localhost:5000` with a commented-out production host `erp-sales-backend.onrender.com`), base path, and schemes. It sets up `swagger-autogen` to generate `swagger-output.json` from `server.js`.

*   **`c:\Users\91965\Desktop\WEB\SALES_ERP\models\Purchase.js`** (Timestamp: 3/10/2026, 12:48:47 AM)
    This file defines the Mongoose schema for `Purchase` documents. It includes a nested `purchaseItemSchema` to detail items within a purchase, incorporating property-specific fields like `propertyType`, `projectName`, and detailed `area` (plot, built-up, carpet area). The main `purchaseSchema` includes fields for vendor, purchase number, date, GSTIN validation, payment status, various taxes, payment modes, and associations with `Company`, `Branch`, and `User` models. It features extensive indexing for efficient queries and a static method `generatePurchaseNo` for auto-generating sequential purchase numbers.

*   **`c:\Users\91965\Desktop\WEB\SALES_ERP\models\Sales.js`** (Timestamp: 3/10/2026, 12:49:07 AM)
    Similar to `Purchase.js`, this file defines the Mongoose schema for `Sales` documents, using an identical `salesItemSchema` for detailed item properties. The main `salesSchema` tracks customer details, invoice numbers, date, items, financial breakdowns, and payment information. It expands payment modes to include `EMI` and adds a comprehensive `paymentDetails` object for specific transaction types (UPI, Bank, Cheque, Card). Like `Purchase`, it includes extensive indexing and a static method `generateInvoiceNo` for auto-generating invoice numbers.

*   **`c:\Users\91965\Desktop\WEB\SALES_ERP\routes\purchases.js`** (Timestamp: 3/10/2026, 12:49:34 AM)
    This file defines the API routes for managing purchases. It implements full CRUD operations with role-based access control (`admin`, `branch`, `user`, `user-panel`), pagination, and advanced filtering capabilities (search, payment status, date range). A key feature in the POST route allows for the creation of new `Item` records directly during a purchase, if `itemId` is not provided but `itemCode` and `itemName` are. It also includes intricate item stock management logic: incrementing `currentStock` and setting `status` to 'Available' upon creation, and intelligently reverting old item stock before updating new items in a PUT request. Notifications are sent for purchase creation and updates.

*   **`c:\Users/91965/Desktop/WEB/SALES_ERP/routes/sales.js`** (Timestamp: 3/10/2026, 12:49:56 AM)
    This file defines API routes for managing sales, mirroring the structure of `purchases.js` with CRUD operations, role-based access control, pagination, and filtering. Sales creation (POST) includes validation to prevent selling already "Sold" items and updates `Item` status to 'Sold' (if paid) or 'Booked' (otherwise), setting `currentStock` to 0. Update (PUT) logic restores previous item status to 'Available' and `currentStock` to 1 before applying new item statuses. The payment patch route automatically updates item status to 'Sold' if the sale becomes fully 'Paid'. The file also indicates an upcoming soft-delete route for sales. Notifications are integrated for sale creation and updates.

### Patterns and Recurring Elements:

*   **Concentrated Development:** All changes occurred within a 45-minute window on the same date, indicating a focused coding session.
*   **ERP Core Modules:** The changes define and implement core "Purchase" and "Sales" modules, crucial for an ERP system.
*   **Mongoose Schema Consistency:** Both `Purchase` and `Sales` models share a consistent structure for item details (including property-specific `area` breakdown), financial fields, and system-level metadata (company, branch, createdBy, timestamps, `isActive`, `isDeleted`).
*   **Automated Numbering:** Both `Purchase` and `Sales` models include static methods (`generatePurchaseNo`, `generateInvoiceNo`) to ensure unique, sequential numbering based on company scope.
*   **API Route Structure:** The API routes for both purchases and sales follow a standard RESTful pattern, leveraging shared middleware for authentication, role-based authorization, pagination, and company/branch-specific filtering.
*   **Real-time Notifications:** Both `purchases.js` and `sales.js` integrate `sendNotification` for actions like creation and updates, indicating a real-time feedback mechanism for users.
*   **Comprehensive Item Stock Management:** A significant recurring pattern is the detailed management of `Item` `currentStock` and `status` across both purchase and sales cycles. Purchases increment stock and mark items as 'Available', while sales decrement stock (to 0) and mark items as 'Sold' or 'Booked'. Updates to either transaction type involve carefully reverting old item states before applying new ones to maintain inventory integrity.
*   **Property-Oriented Fields:** The inclusion of `propertyType`, `projectName`, `plotArea`, `builtUpArea`, and `carpetArea` in both purchase and sales item schemas strongly suggests that this ERP system is tailored for a real estate or property management business.
*   **Robust Filtering and Pagination:** Both GET endpoints for listing purchases and sales implement advanced filtering by search terms, payment status, active status, and date ranges, along with comprehensive pagination.
*   **Detailed Payment Handling:** Both `Purchase` and `Sales` schemas include detailed payment status, amount paid, and balance, with `Sales` further expanding to detailed `paymentDetails` for various payment methods.