# Activity Summary for 3/11/2026

## 1:57:45 AM
The provided log details changes across three JavaScript files in the `IPSuite-Web` project, specifically within manager-related components and Redux actions.

### File-Specific Updates:

1.  **`c:\Users\91965\Desktop\MikeLegal\IPSuite-Web\src\components\managerComponents\notification\Notification.jsx`**
    *   **Initial state (3/10/2026, 12:15:50 PM):** The `Notification` component (a React class component) is initialized with a `noTagToggle` state set to `false` and `selectedFilters` for both `trademark` and `patent` types which include `trademarkTags: []` and `trademarkNOtags: []`.
    *   **Minor updates (3/10/2026, 12:17:17 PM - 12:25:38 PM):** The code remains largely identical across multiple timestamps, suggesting minor edits, whitespace changes, or saving operations without functional alterations being captured. The `handleNoTag` function updates the `noTagToggle` state and calls `this.changeStateValue_L2` to set `trademarkNOtags` in `selectedFilters` to `["no tag"]` or `[]` based on the toggle state.
    *   **Change in `resetFilter` logic (3/10/2026, 2:49:25 PM):** The `resetFilter` method is updated to also set `noTagToggle: false` in the component's state, in addition to resetting filter ranges and selected filters.
    *   **Refined `resetFilter` for tags (3/10/2026, 2:50:57 PM - 2:51:16 PM):** Further modifications to `resetFilter` explicitly clear `trademarkTags` and `trademarkNOtags` within the `selectedFilters[type]` object when resetting filters.
    *   **`handleNoTag` logic improvement (3/10/2026, 3:31:53 PM - 3:32:40 PM):** The `handleNoTag` function is enhanced to prevent enabling "no tag" filtering if `trademarkTags` are already selected, issuing a warning message. It also consistently sets `trademarkNOtags` to `["no_tag"]` when toggled on.
    *   **Removed `selectedFilters` initialization (3/10/2026, 3:08:48 PM - 3:20:51 PM):** A block within the `constructor` that re-initialized `selectedFilters.trademark` and `selectedFilters.patent` with `trademarkTags: []` and `trademarkNOtags: []` (effectively duplicating and potentially overriding the initial spread from `this.props.tmNotificationAppliedFilters`) was commented out and then removed. The current (final) state in the log shows this block entirely removed.

2.  **`c:\Users\91965\Desktop\MikeLegal\IPSuite-Web\src\redux\actions\managerActions\profile\actions.js`**
    *   **Initial state (3/10/2026, 12:33:36 PM):** The `getNotification` action creator constructs parameters for an API call, including `tags` and `no_tags` based on `filters.trademarkTags` and `filters.trademarkNOtags`. The logic for `no_tags` appears to be: if `tags` is not empty, `no_tags` is null; otherwise, if `no_Tags` has length, `no_tags` is true.
    *   **Refined `no_tags` logic (3/10/2026, 3:38:36 PM - 3:40:51 PM):** The `getNotification` action's `params` for `tags` and `no_tags` are repeatedly adjusted, particularly the `no_tags` condition. The final modification sets `no_tags` to `null` if `tags` exist, and `true` only if `tags` is empty and `no_Tags` has items. This indicates a focus on ensuring correct mutual exclusivity or precedence for tags vs. no-tags filtering in the API request.

3.  **`c:\Users\91965\Desktop\MikeLegal\IPSuite-Web\src\components\commonComponents\FilterComponents\FilterSearchBasic.jsx`**
    *   **Debugging additions (3/10/2026, 1:18:06 PM - 1:33:44 PM):** Multiple `console.log` statements are added, specifically within the `filteredData` logic for `type === "name" || type === "tags" || type === "noTag"`. These logs (`console.log("dattta", data)`, `console.log("ohhhhh", checkValue(obj))`, `console.log("Filter Debug ->", { ... })`, `console.log("filteredData", filteredData)`) were introduced, refined, and later removed, indicating a debugging session for filtering logic.
    *   **Checkbox `disabled` state update (3/10/2026, 1:54:53 PM):** The "No Tag" checkbox's `disabled` property is updated. It becomes disabled if `selectedFilters?.[filter_key]?.length > 0` (meaning other tags are selected) and `!noTagToggle` (it's currently unchecked), effectively preventing simultaneous selection of specific tags and the "No Tag" option.
    *   **Checkbox `checked` state update (3/10/2026, 3:53:48 PM):** The "No Tag" checkbox's `checked` property is modified from just `noTagToggle` to `noTagToggle || selectedFilters?.trademarkNOtags?.length > 0`. This ensures the checkbox reflects its checked state even if `noTagToggle` is false but `trademarkNOtags` is still populated in `selectedFilters`.

4.  **`c:\Users\91965\Desktop\MikeLegal\IPSuite-Web\src\components\managerComponents\trademark\Manage-Trademark.jsx`**
    *   **No significant functional changes are recorded in the provided snippets for this file.** The timestamps and code content are identical across the entries, implying only saving operations occurred.

5.  **`c:\Users\91965\Desktop\MikeLegal\IPSuite-Web\src\components\managerComponents\trademark\YourTrademak\TotalTrademark.jsx`**
    *   **`componentDidMount` logic update (3/10/2026, 6:11:11 PM):** The `componentDidMount` lifecycle method is changed to call `this.applyFilter()` immediately after setting `selectedFilters` if query parameters (`active`, `inactive`, `registered`) are present. Previously, it would call `getTrademark` with default filters, then potentially call `applyFilter` in a `.then()` block (which was commented out). This makes the initial filter application more direct when query parameters exist.
    *   **Minor modifications without functional change (3/10/2026, 6:14:15 PM - 6:26:45 PM):** Similar to other files, several timestamps show identical content, indicating minor saves or formatting changes.

6.  **`c:\Users\91965\Desktop\MikeLegal\IPSuite-Web\src\components\managerComponents\to-do-task\TodoList.jsx`**
    *   **"Expires On" text changed (3/11/2026, 1:42:36 AM):** The label "Expires On :" is changed from "Expired On :" in the `render` method within the `Col` displaying task details. This is a minor text correction.

### Patterns and Recurring Elements:

*   **Ant Design (antd) Components:** All reviewed files heavily utilize Ant Design components like `Layout`, `Row`, `Col`, `Typography`, `Table`, `Switch`, `Input`, `Select`, `Menu`, `Dropdown`, `Button`, `Badge`, `message`, `notification`, `Checkbox`, `Card`, `Empty`, `Pagination`, `Spin`, and `Tooltip`. This indicates a consistent UI framework across the application.
*   **Redux for State Management:** Components use `connect` from `react-redux` and dispatch actions (`getNotification`, `getTrademark`, `deleteToDoTask`, `deleteAllToDoTask`, `logOut`, etc.) to manage application state. Actions often involve `LOADING`, `SUCCESS`, and `ERROR` statuses, suggesting a pattern for asynchronous data fetching and state updates.
*   **Common Utility Functions:** `checkValue` (from `../../../functions/commonFunctions/checkValueFunctions`) is frequently used to safely render data or check for truthiness, preventing issues with `null` or `undefined` values. `changeStateValue_L0`, `changeStateValue_L1`, `changeStateValue_L2` are also commonly bound to `this`, suggesting a standardized way to update nested state.
*   **Filtering Logic:** There's a recurring pattern in `applyFilter` and `resetFilter` methods across `Notification.jsx` and `TotalTrademark.jsx`. These methods check for filter criteria, call Redux actions (`getNotification` or `getTrademark`) with `filterFlag` and `filters`, and update the component's `selectedFilters` state.
*   **"No Tag" Feature:** A specific "no tag" filtering functionality (`noTagToggle`, `trademarkNOtags`, `handleNoTag`) is implemented in both `Notification.jsx` and `FilterSearchBasic.jsx`, indicating a common requirement to filter items that explicitly lack any tags. The logic ensures that "no tag" cannot be selected simultaneously with other specific tags.
*   **Session Storage Usage:** `sessionStorage.setItem` is frequently used to store IDs and application numbers when navigating to profile pages (e.g., `handleTrademarkProfile`, `handlePatentProfile`).
*   **Date Formatting/Handling:** The presence of `DateRangePicker` and `standardizeDate` implies date selection and formatting are common tasks.
*   **API Calls with Axios:** All data fetching actions (`getDashboard`, `getNotification`, `getTrademark`, `getCustomNotification`, etc.) consistently use `Axios.get` to `MGR_RT_URL` endpoints, including an `Authorization` header with a token from `localStorage`. Error handling often involves `checkAuthorisation`.
*   **Debugging Console Logs:** Several changes, particularly in `FilterSearchBasic.jsx`, involved adding and removing `console.log` statements for debugging purposes, highlighting iterative development and issue resolution.
*   **Focus on Trademark/Patent Management:** The file paths and component/action names (`managerComponents`, `trademarkDashboard`, `patentNotification`, `TM_DASHBOARD`, etc.) indicate that the system is primarily concerned with managing trademark and patent related data.

## 12:36:00 PM
The provided log details changes across two React/Redux application files, primarily focusing on a To-Do List component and the main application entry point. All recorded changes occurred on March 11, 2026.

**File: `c:\Users\91965\Desktop\MikeLegal\IPSuite-Web\src\components\managerComponents\to-do-task\TodoList.jsx`**

*   **Timestamp: 3/11/2026, 10:11:24 AM**
    *   This file defines the `TodoTask` React component, which manages a user's to-do list.
    *   It imports a wide range of Ant Design components for UI, Redux for state management, and specific manager actions for to-do tasks (get, delete, delete all).
    *   The component handles task display, filtering by status (show all, pending/expired switch), and priority (All, High, Medium, Low).
    *   It includes functionality for adding/editing tasks (via `CreateTask` component), deleting individual tasks, deleting all tasks, and downloading task `ics_file`s.
    *   Task items are rendered in cards, with visual cues (color bands) based on their status (Expired) and priority (High, Medium, Low).
    *   The `Expires On` label is used to display the task's expiry date.
    *   Pagination is implemented for displaying tasks.
    *   Redux actions `getToDoTask`, `deleteToDoTask`, `deleteAllToDoTask`, and `logOut` are mapped for dispatch.

*   **Timestamp: 3/11/2026, 10:13:18 AM**
    *   A minor textual update was made in the `render` method for displaying task details.
    *   The label for the task's expiry date was changed from `<label>Expires On : </label>` to `<label>Expired On : </label>`.
    *   No other functional or structural changes were introduced in this update.

**File: `c:\Users\91965\Desktop\MikeLegal\IPSuite-Web\src\index.jsx`**

*   **Timestamp: 3/11/2026, 12:33:31 PM**
    *   This file serves as the main entry point for the React application.
    *   It initializes the Redux store and wraps the main `App` component with `Provider` (for Redux) and `Router` (for React Router).
    *   It imports necessary libraries like `react`, `react-redux`, `react-router-dom`, and external services like Sentry and Mixpanel.
    *   Sentry error tracking is included but currently commented out, along with `setTag` calls for user information.
    *   Mixpanel analytics is initialized, with different project tokens used based on the `ROOT_URL` (distinguishing between staging and production environments).
    *   It imports global stylesheets (`index.css`, `main.scss`).
    *   The service worker is unregistered.

**Patterns and Recurring Elements:**

*   **Ant Design and Redux:** The project heavily utilizes Ant Design components for its UI and Redux for state management, which is evident in the `TodoList.jsx` file's structure and the `index.jsx`'s Redux setup.
*   **Common Constants:** The use of `LOADING`, `SUCCESS`, and `ERROR` constants from `commonConstants/generalConstants` for managing API call statuses is a recurring pattern in the `TodoList.jsx` component.
*   **Date-Specific Development:** All logged changes occurred on a single day (March 11, 2026), indicating focused development or deployment activity.
*   **External Service Integration:** The integration of Sentry (though commented out) and Mixpanel points to a focus on application monitoring and analytics.