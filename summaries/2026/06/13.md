# Activity Summary for 6/13/2026

## 12:53:02 PM
The `.gitignore` file, last updated on 6/13/2026 at 10:55:32 AM, includes standard exclusions for Node.js projects such as `node_modules`, `coverage`, and `build` directories, along with macOS-specific `.DS_Store` files and various debug logs. Crucially, it also explicitly ignores `.env` files, including local, development, test, and production variants, which is a common practice for preventing sensitive configuration from being committed to version control.

The `.env` files at timestamps 6/13/2026, 11:33:25 AM and 6/13/2026, 11:56:30 AM contain environment variables for various services like MongoDB connection URIs, JWT secrets, SMTP credentials, Razorpay API keys, and Cloudinary API details. As per instructions, a detailed summary of their content is not provided to protect sensitive information.

The `controllers/admin/adminAuth.js` file, last modified on 6/13/2026 at 11:58:10 AM, outlines the core authentication and profile management logic for administrators.
Key functionalities include:
*   **Initialization and Setup**: It imports necessary modules like `jsonwebtoken`, `bcryptjs`, `nodemailer`, and an `Admin` Mongoose schema. It defines constants for a default `ADMIN_EMAIL` and `ADMIN_DEFAULT_PASSWORD`, used for initial setup and system-level operations. A `createAdminMailTransporter` function is present, configured to use environment variables for SMTP authentication.
*   **Admin Document Management**: The `getOrCreateAdminDoc` function ensures a default admin account exists in the database. If not found, it creates one with a hashed default password. This pattern ensures the super admin account is available upon application boot or first access.
*   **Profile Management**: `getAdminProfile` allows fetching the admin's details (excluding password). `updateAdminProfile` provides functionality to modify details such as name, department, gender, and profile picture.
*   **Password Security**: `changeAdminPassword` enables an admin to update their password after verifying the current one using bcrypt. Passwords are consistently hashed with bcrypt before storage.
*   **Forgot Password Flow**:
    *   `sendAdminForgotPasswordOtp` generates a 6-digit OTP, stores it along with an expiry timestamp (10 minutes) in the admin's database record, and sends it to the admin's registered email using Nodemailer. This function strictly validates that the request email matches `ADMIN_EMAIL`.
    *   `resetAdminPassword` verifies the provided OTP against the stored one and its expiry. Upon successful validation, it updates the admin's password with a new hashed one and clears the OTP fields.
*   **Admin Login**: `loginAdmin` authenticates the admin using email and password, comparing the provided password with the hashed one stored in the database. Upon successful authentication, it generates and returns a JSON Web Token (JWT) with a 1-day expiry, along with the admin's role and subrole.

**Patterns and Recurring Elements**:
*   **Environment Variable Dependence**: The application heavily relies on environment variables (`process.env`) for sensitive configurations like JWT secrets, MongoDB URIs, and SMTP credentials, which is a robust security practice.
*   **Consistent Timestamp Format**: All log entries use the format `MM/DD/YYYY, HH:MM:SS AM/PM`.
*   **Security Best Practices**: Password hashing with `bcryptjs`, OTP-based password reset, and JWT for session management are consistently applied in the authentication logic.
*   **Error Handling**: Each API endpoint includes try-catch blocks to gracefully handle and respond to errors, providing success/failure flags and detailed messages.
*   **Database Interaction**: Mongoose is used for interacting with MongoDB, with operations like `findOne`, `create`, `findByIdAndUpdate`, and `findOneAndUpdate` being prevalent.
*   **Centralized Admin Logic**: All core admin authentication and profile management logic is consolidated within the `adminAuth.js` controller.