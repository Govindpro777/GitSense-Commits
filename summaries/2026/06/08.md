# Activity Summary for 6/8/2026

## 12:34:55 PM
The primary file updated is `c:\Users\91965\Desktop\MikeLegal\clm3\clm-frontend\shared\src\lib\config\index.ts`.

**Changes at 6/8/2026, 11:40:12 AM:**
This timestamp shows an initial configuration setting up various service URLs. Key configurations included:
*   `ROOT_URL`, `DOCCHAT_ROOT_URL`, and `ACCOUNT_URL` were set to staging domains (`*-staging.mikelegal.com`).
*   Authentication URLs like `AUTH_URL` and `MANAGEMENT_URL`, along with the `issuer`, pointed to `account.mikelegal.com` and `login.mikelegal.com` respectively.
*   Zitadel `clientId` was '333807352648105987'.
*   Digio configuration was present with a production URL (`https://app.digio.in`) and labeled "Actual credentials" for the client ID and secret.
*   A significant portion of the file was commented out, containing an alternative set of staging and sandbox configurations.

**Changes at 6/8/2026, 11:41:38 AM:**
A subsequent update occurred shortly after, significantly altering the configuration to align with a more consistent staging/sandbox environment.
*   All core service URLs (`ROOT_URL`, `DOCCHAT_ROOT_URL`, `ACCOUNT_URL`) remained consistent with their staging counterparts.
*   A new WebSocket URL, `SUPER_DOC_COLLAB_URL`, was introduced, pointing to `wss://superdoc-collab-staging.internal.mikelegal.com`.
*   Authentication-related URLs were updated: `AUTH_URL` changed to `https://account-staging.mikelegal.com`, and the `issuer` became `https://auth-staging.mikelegal.com`. The `MANAGEMENT_URL` and `MANAGEMENT_CLIENT_ID` were removed.
*   The Zitadel `clientId` was updated to '333600799483560196'.
*   The Digio configuration was switched to use the sandbox URL (`https://ext-gateway.digio.in`) and reflected "Sandbox credentials" for its client ID and secret, replacing the previously active "Actual credentials".

**Patterns and Recurring Elements:**
The most significant pattern observed is a rapid transition from a potentially mixed or production-oriented configuration to a fully staging and sandbox environment setup. This is evident through:
*   The consistent use of `-staging` subdomains for most service URLs.
*   The explicit shift in Digio configuration from "Actual credentials" pointing to a live service to "Sandbox credentials" pointing to an external gateway.
*   The introduction of a new collaboration service endpoint (`SUPER_DOC_COLLAB_URL`) also configured for a staging environment.
The changes suggest a preparation or deployment of a specific staging environment, ensuring all integrated services and authentication flows point to their respective staging or sandbox instances.

## 1:19:59 PM
The `c:\Users\91965\Desktop\Society\Society_Services_Backend\models\vendorSchema.js` file, last updated on 6/8/2026 at 10:45:14 AM, defines a comprehensive Mongoose schema for `Vendor` entities.

Key information from this change includes:

*   **Core Vendor Information**: The schema captures essential details such as `name`, `email` (unique, sparse, with regex validation), `password`, `contactNumber` (unique, with regex validation), `profilePicture`, `businessName`, `experience`, and `gstNumber`.
*   **Address and Location**: It includes a detailed `address` object (buildingNumber, locality, landmark, city, state, pincode, formattedAddress) and a `location` field using GeoJSON "Point" type with `coordinates` (longitude, latitude). A `2dsphere` index is applied to the `location` field for geospatial queries, and a `pre('save')` hook is implemented to ensure `location.coordinates` are valid before saving, preventing MongoDB geo-index failures by setting invalid locations to `undefined`.
*   **Service and Job Management**: Vendors can list `services` (an array of `ObjectId`s referencing a `Service` model) and their `jobHistory` (an array of `ObjectId`s referencing a `Job` model).
*   **Ratings and Status**: Fields for `averageRating` and `totalRatings` are included. `isProfileCompleted` tracks profile completeness.
*   **Payment Details**: Comprehensive `paymentMethods` (UPI and Card details) and a `lastPayments` array (tracking date, amount, method, transaction ID, reason, and status) are defined.
*   **Administrative and Business Logic**:
    *   `role` is set to "vendor" and is immutable.
    *   `isApproved` and `isBlacklisted` fields (with a `blacklistReason`) are present for administrative control.
    *   `subscription` status (isActive, expiresAt) is managed.
    *   `vendorCode` (unique, sparse, indexed) and `vendorReferenceId` (auto-generated, unique, required) are introduced for internal tracking.
*   **Timestamping**: The schema enables Mongoose's `timestamps` option, automatically adding `createdAt` and `updatedAt` fields to documents.

This schema demonstrates a robust design for managing vendor data within a service-based backend, incorporating validation, geospatial capabilities, payment tracking, and administrative controls.

## 1:21:00 PM
The changes log details updates to two frontend React components within the `MikeCLM` application on 6/8/2026.

### File-Specific Updates:

1.  **`c:\Users\91965\Desktop\MikeLegal\clm-frontend\apps\MikeCLM\src\containers\Drafts\components\SuggestionModeNotice.tsx`**
    *   This file experienced a rapid series of updates between 11:50:36 AM and 11:51:33 AM.
    *   **Initial State (11:50:36 AM):** The component, `SuggestionModeNotice`, was a modal designed to inform users about limitations in "track mode." The primary message was that "Deleting text one character at a time may not work as expected," recommending selecting text for deletion and noting that the issue was being worked on.
    *   **First Update (11:50:55 AM):** The main heading (`h2`) of the modal was changed from "Things to Note while making changes in track mode -" to "Editor Notice."
    *   **Second Update (11:51:09 AM):** The introductory paragraph below the heading was updated to "Things to Note while making changes in track mode -", essentially moving the previous heading's content to a descriptive paragraph.
    *   **Third Update (11:51:21 AM):** The specific instruction regarding deletion was rephrased for clarity. It changed from a general recommendation to "To remove content, please select the required text and delete it in a single action," to a more detailed explanation: "When deleting entire word, it is recommended to select the entire word and delete as currently deleting by pressing backspace may show individual letters being deleted."
    *   **Final Update (11:51:33 AM):** The generic acknowledgment of active work on improvement was replaced with a more specific known issue: "Sometimes, when track changes are accepted it may show accepted but it may still reflect as not accepted. We are working on fixing this issue."
    *   Overall, these changes refined the modal's title and provided more detailed and specific information about known limitations and workarounds in the editor's track mode functionality, along with an explicit mention of a bug related to accepting track changes.

2.  **`c:\Users\91965\Desktop\MikeLegal\clm-frontend\apps\MikeCLM\src\components\UI\SearchBar2\SearchBar2.tsx`**
    *   This file was updated or created at 12:28:25 PM.
    *   It defines a `SearchBar2` React functional component that includes an Ant Design-like `Input` field (likely a custom `Input` from `./ui/input`) with a `Search` icon (from `lucide-react`), along with "Apply Filters" and "Reset" buttons.
    *   The search input handles `searchValue` and `onSearchChange`, and triggers `onApplyFilters` when the 'Enter' key is pressed.
    *   The "Apply Filters" button is conditionally disabled if the `searchValue` is empty. This component provides a reusable search interface with filtering capabilities.

### Patterns and Recurring Elements:

*   **Timestamp Concentration:** All recorded changes occurred on the same date, 6/8/2026. The updates to `SuggestionModeNotice.tsx` were rapid and sequential within a minute, suggesting a concentrated effort to refine the textual content of a user-facing notification. The `SearchBar2.tsx` update occurred later on the same day.
*   **React Functional Components:** Both files define React functional components, indicating a consistent development approach.
*   **UI Component Library Usage:** The project uses `antd` for modals (as seen in `SuggestionModeNotice`) and custom UI components (like `Button` and `Input` from `./ui/` in `SearchBar2`), suggesting a hybrid approach to UI development.
*   **Utility-First CSS:** The code snippets consistently use utility classes (e.g., `flex`, `text-2xl`, `font-bold`, `bg-blue-600`), strongly implying the use of a framework like Tailwind CSS for styling.
*   **Focus on User Experience:** The changes reflect an ongoing effort to improve the user experience, either by clarifying editor behavior and known limitations (`SuggestionModeNotice`) or by providing common UI patterns like a search bar (`SearchBar2`).

## 2:19:59 PM
A new Mongoose schema, `vendorSchema.js`, has been defined to model vendor information.

**File-specific Updates:**

*   **`c:\Users\91965\Desktop\Society\Society_Services_Backend\models\vendorSchema.js` (Timestamp: 6/8/2026, 10:45:14 AM)**
    *   This file introduces the `Vendor` schema for a backend service.
    *   It captures comprehensive details about a vendor, including:
        *   **Basic Information:** `name`, `businessName`, `profilePicture`, `email` (with regex validation and unique constraint), `password`, `contactNumber` (unique with regex validation), `otp`, `isProfileCompleted`.
        *   **Address:** A nested object containing `buildingNumber`, `locality`, `landmark`, `city`, `state`, `pincode`, and `formattedAddress`, all with default "Not Given" or "000000".
        *   **Ratings:** `averageRating` and `totalRatings`, defaulting to 0.
        *   **Proof/Experience:** `idProof` and `experience` (defaulting to ">1 years").
        *   **Services:** An array of `ObjectId`s referencing a `Service` model, indicating predefined services the vendor offers.
        *   **Location:** A GeoJSON `Point` type with `coordinates` (longitude, latitude) and `formattedAddress`, indexed as a `2dsphere` for geospatial queries. A `pre('save')` hook is implemented to remove invalid or incomplete `location` objects before saving, preventing MongoDB geo-key extraction failures.
        *   **Payment Methods:** Detailed nested objects for `UPI` (uPIID, uPIApp) and `Card` (cardType, cardNumber, cardHolderName, cardExpiry, cardCVV), with default "Not Given" or null values.
        *   **Last Payments:** An array of payment records, each including `paymentDate`, `amount`, `paymentMethod`, `transactionId`, `paymentReason`, and `paymentStatus`.
        *   **Role:** Defaults to "vendor" and is immutable.
        *   **Job History:** An array of `ObjectId`s referencing a `Job` model.
        *   **Business Details:** `gstNumber`.
        *   **Subscription:** `isActive` and `expiresAt` for managing vendor subscriptions.
        *   **Unique Identifiers:** `vendorCode` (unique, sparse, indexed) and `vendorReferenceId` (required, unique, with an auto-generated default value like `VELRE-XXXXXXXX`).
        *   **Admin Approval/Blacklisting:** `isApproved` (default false), `isBlacklisted` (default false), and `blacklistReason` fields.
    *   The schema also includes `timestamps: true` for automatic `createdAt` and `updatedAt` fields.

**Patterns and Recurring Elements:**

*   **Mongoose Schema Usage:** Consistent use of Mongoose for defining data models.
*   **Detailed Field Definitions:** Many fields include specific types, `required` constraints, `default` values, and custom validation (e.g., regex for `email` and `contactNumber`).
*   **Referencing Other Models:** The schema frequently references other Mongoose models (e.g., `Service`, `Job`) using `mongoose.Schema.Types.ObjectId` and `ref`.
*   **Geospatial Data Handling:** The `location` field demonstrates robust handling of geospatial data, including `2dsphere` indexing and a `pre-save` hook for data integrity.
*   **Comprehensive Business Logic Fields:** The schema incorporates fields for various aspects of a service-based business, such as payment details, subscriptions, job history, and admin approval workflows.
*   **Unique Identifiers:** Multiple unique identifiers are employed (`email`, `contactNumber`, `vendorCode`, `vendorReferenceId`) to ensure data integrity and facilitate lookups.
*   **Structured Nested Objects:** Complex data like `address`, `paymentMethods`, and `lastPayments` are organized into nested objects and arrays for better data representation.

## 3:20:02 PM
The provided log details changes to a Mongoose schema file.

**File:** `c:\Users\91965\Desktop\Society\Society_Services_Backend\models\vendorSchema.js`
**Timestamp:** 6/8/2026, 10:45:14 AM

This file defines the `vendorSchema` for a MongoDB collection, outlining the structure for vendor data. Key aspects of the schema include:

*   **Personal and Business Details:** Fields such as `name`, `businessName`, `profilePicture`, `email`, `password`, `otp`, and `isProfileCompleted`. The `email` field is unique, sparse, trimmed, and includes regex validation.
*   **Address Information:** A nested `address` object stores detailed location data including `buildingNumber`, `locality`, `landmark`, `city`, `state`, `pincode`, and `formattedAddress`, mostly with default "Not Given" values.
*   **Ratings and Experience:** `averageRating`, `totalRatings`, and `experience` fields track vendor performance and tenure.
*   **Contact and Identification:** `contactNumber` is required, unique, trimmed, and validated with a regex pattern. `idProof` and `gstNumber` fields are also included.
*   **Services and Job History:** A `services` array stores references to a `Service` model, indicating the services offered by the vendor. `jobHistory` similarly references a `Job` model.
*   **Geo-Location:** A `location` field uses GeoJSON `Point` type with `coordinates` (longitude, latitude) and `formattedAddress`. A `2dsphere` index is applied to this field for geospatial queries.
*   **Payment Information:** Comprehensive `paymentMethods` are defined, supporting both `UPI` (with `uPIID`, `uPIApp`) and `Card` (with `cardType`, `cardNumber`, `cardHolderName`, `cardExpiry`, `cardCVV`). An array `lastPayments` logs past transactions with details like `paymentDate`, `amount`, `paymentMethod`, `transactionId`, `paymentReason`, and `paymentStatus`.
*   **Administrative and Subscription Details:** Fields like `role` (defaulting to "vendor" and immutable), `isApproved`, `isBlacklisted`, `blacklistReason`, and `subscription` (with `isActive` and `expiresAt`) manage the vendor's account status.
*   **Unique Identifiers:** `vendorCode` is unique, sparse, and indexed, while `vendorReferenceId` is required, unique, and automatically generated with a `VELRE-` prefix.
*   **Timestamps:** The schema is configured to automatically add `createdAt` and `updatedAt` fields.
*   **Pre-save Hook for Location Data Integrity:** A `pre('save')` hook ensures that if the `location` field exists, its `coordinates` are a valid array of two numbers. If the coordinates are invalid, the `location` field is set to `undefined` to prevent MongoDB geo-indexing errors.

**Patterns/Recurring Elements:**
*   Extensive use of default values (e.g., "Not Given", `false`, `0`).
*   Frequent application of Mongoose validation options like `required`, `unique`, `trim`, `match` (with regex), and `enum`.
*   Nested object structures are used to organize related data (e.g., `address`, `location`, `paymentMethods`).
*   Arrays of embedded documents or references are used for lists (e.g., `services`, `lastPayments`, `jobHistory`).
*   References to other Mongoose models (`Service`, `Job`) are used to link related data.
*   The schema includes mechanisms for both data validation and integrity, notably the pre-save hook for geo-location data.