# Activity Summary for 6/8/2026

## 12:34:55 PM
The primary file updated is `c:\Users\91965\Desktop\MikeLegal\clm3\clm-frontend\shared\src\lib\config\index.ts`.

**Changes at 6/8/2026, 11:40:12 AM:**
This timestamp shows an initial configuration setting up various service URLs. Key configurations included:
*   `ROOT_URL`, `DOCCHAT_ROOT_URL`, and `ACCOUNT_URL` were set to staging domains (`*-staging.mikelegal.com`).
*   Authentication URLs like `AUTH_URL` and `MANAGEMENT_URL`, along with the `issuer`, pointed to `account.mikelegal.com` and `login.mikelegal.com` respectively.
*   Zitadel `clientId` was '333807352648105987'.
*   Digio configuration was present with a production URL (`https://app.digio.in`) and labeled "Actual credentials" for the client ID and secret.
*   A significant portion of the file was commented out, containing an alternative set of staging and sandbox configurations.

**Changes at 6/8/2026, 11:41:38 AM:**
A subsequent update occurred shortly after, significantly altering the configuration to align with a more consistent staging/sandbox environment.
*   All core service URLs (`ROOT_URL`, `DOCCHAT_ROOT_URL`, `ACCOUNT_URL`) remained consistent with their staging counterparts.
*   A new WebSocket URL, `SUPER_DOC_COLLAB_URL`, was introduced, pointing to `wss://superdoc-collab-staging.internal.mikelegal.com`.
*   Authentication-related URLs were updated: `AUTH_URL` changed to `https://account-staging.mikelegal.com`, and the `issuer` became `https://auth-staging.mikelegal.com`. The `MANAGEMENT_URL` and `MANAGEMENT_CLIENT_ID` were removed.
*   The Zitadel `clientId` was updated to '333600799483560196'.
*   The Digio configuration was switched to use the sandbox URL (`https://ext-gateway.digio.in`) and reflected "Sandbox credentials" for its client ID and secret, replacing the previously active "Actual credentials".

**Patterns and Recurring Elements:**
The most significant pattern observed is a rapid transition from a potentially mixed or production-oriented configuration to a fully staging and sandbox environment setup. This is evident through:
*   The consistent use of `-staging` subdomains for most service URLs.
*   The explicit shift in Digio configuration from "Actual credentials" pointing to a live service to "Sandbox credentials" pointing to an external gateway.
*   The introduction of a new collaboration service endpoint (`SUPER_DOC_COLLAB_URL`) also configured for a staging environment.
The changes suggest a preparation or deployment of a specific staging environment, ensuring all integrated services and authentication flows point to their respective staging or sandbox instances.